
function maxarr(arr)
{
    return Math.max.apply(null,arr)
}
const arr=[123,80,66,124,180,101];
console.log(maxarr(arr))

function myArrayMax(arr) {
    return Math.max.apply(null, arr);
    }
    const points = [40, 100, 1, 5, 25, 10];
    myArrayMax(points);
    console.log(myArrayMax(points));